// Definindo uma lista
let = [2,4,6,8,10]

// Percorrendo a lista
for(let i = 0; < numeros.length; i++){
    if(numeros[i] % 2 === 0){
      console.log(numeros[i] + " é um numero par")
    }else{
      console.log(numero[i] + " é um numero ímpar")
    }
  }
